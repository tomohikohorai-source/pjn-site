import os
import datetime
import feedparser
import requests
import json
import time

# --- 設定 ---
API_KEY = os.environ.get("GEMINI_API_KEY", "").strip()
MODEL_NAME = "gemini-2.0-flash-lite"
API_URL = f"https://generativelanguage.googleapis.com/v1/models/{MODEL_NAME}:generateContent?key={API_KEY}"

POSTS_DIR = "src/pages/posts"
os.makedirs(POSTS_DIR, exist_ok=True)

# ニュースソース（確実に記事URLが含まれるNationニュース）
RSS_URLS = ["https://www.thestar.com.my/rss/news/nation"]

def ask_ai(title, summary, link):
    print(f"AI翻訳中: {title}")
    
    # 指示を具体化し、出力にジャンルを含めるよう徹底
    prompt = f"""
    あなたはペナン在住日本人向けのニュース編集長です。
    以下の英語ニュースを、日本人（特に子育て家庭や留学生）向けに日本語で全文翻訳して。
    
    【ニュース】
    タイトル: {title}
    内容: {summary}

    【出力ルール】
    ・1行目は必ず「ジャンル：〇〇」とする（教育、生活、グルメ、おでかけ、暮らし、重要、エンタメ、お得 のいずれか）
    ・2行目から本文を開始し、読みやすく改行を入れる。
    """

    payload = {"contents": [{"parts": [{"text": prompt}]}]}

    for attempt in range(3):
        try:
            response = requests.post(API_URL, headers={'Content-Type': 'application/json'}, data=json.dumps(payload))
            if response.status_code == 200:
                data = response.json()
                content = data["candidates"][0]["content"]["parts"][0]["text"]
                
                # ジャンルを1行目から抽出
                lines = content.strip().split('\n')
                genre = "暮らし" # デフォルト
                if "ジャンル：" in lines[0]:
                    genre = lines[0].replace("ジャンル：", "").strip()
                    body = "\n".join(lines[1:])
                else:
                    body = content

                return f"""---
title: "{title}"
date: "{datetime.date.today()}"
category: "{genre}"
---
<div class="genre-label">ジャンル：{genre}</div>
<h3>【内容（全文翻訳）】</h3>

{body}

<a href="{link}" target="_blank" rel="noopener noreferrer" class="source-link">🔗 参照元記事（英語サイト）を確認する</a>
"""
            elif response.status_code == 429:
                print(f"⚠️ 制限中... 60秒待機 ({attempt+1}/3)")
                time.sleep(60)
            else:
                return None
        except:
            return None
    return None

# --- メイン実行 ---
print(f"PJN Bot 起動 (モデル: {MODEL_NAME})")

feed = feedparser.parse(RSS_URLS[0])
count = 0

for entry in feed.entries[:5]: 
    # RSSから「記事への直リンク」を取得（ここが修正ポイントです）
    article_link = entry.link 
    
    safe_title = "".join([c for c in entry.title if c.isalnum() or c==' '])[:30].strip().replace(" ", "_")
    filename = os.path.join(POSTS_DIR, f"{datetime.date.today()}-{safe_title}.md")
    
    if os.path.exists(filename): continue

    result = ask_ai(entry.title, entry.summary, article_link)
    if result:
        with open(filename, "w", encoding="utf-8") as f:
            f.write(result)
        print(f"✅ 保存完了: {filename}")
        count += 1
        time.sleep(60) # 1分休憩して制限回避

print(f"完了。作成記事数: {count}")
